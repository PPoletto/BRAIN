node_modules/
target/
dist/
.DS_Store
*.log
.env
.env.*

# Tauri / Rust
src-tauri/target/
src-tauri/gen/
src-tauri/WixTools/
**/*.rs.bk

# IDE
.idea/
.vscode/
*.iml

# Claude Code project config — dev-time tooling, not for the public repo.
.claude/

# Signing keys — must NEVER be in the repo.
*.key
*.pem
brain.pub
brain.key

# OS
Thumbs.db
ehthumbs.db
Desktop.ini

# Build artefacts
.parcel-cache/
.turbo/
*.tsbuildinfo
coverage/

# Dev-only documents — used during development, not for end users.
# Leading "/" anchors these to the repo root, so the runtime template
# files at src-tauri/src/onboarding/templates/{CLAUDE,AGENTS}.md stay
# tracked (those get copied into the user's vault at onboarding time).
/CLAUDE.md
/AGENTS.md
/requirements/
/docs/architecture.md
/docs/implementation-plan.md
/docs/post-mvp-roadmap.md
/docs/adr/
